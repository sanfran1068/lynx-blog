<% if (theme.comment && theme.comment.disqus) { %>
    <%- partial('comment/disqus', { script: true }) %>
<% } else if (theme.comment && theme.comment.duoshuo) { %>
    <%- partial('comment/duoshuo', { script: true }) %>
<% } else if (theme.comment && theme.comment.youyan) { %>
    <%- partial('comment/youyan', { script: true }) %>
<% } else if (theme.comment && theme.comment.facebook) { %>
    <%- partial('comment/facebook', { script: true }) %>
<% } else if (theme.comment && theme.comment.isso) { %>
    <%- partial('comment/isso', { script: true }) %>
<% } else if (theme.comment && theme.comment.changyan && theme.comment.changyan.appid && theme.comment.changyan.conf) { %>
    <%- partial('comment/changyan', { script: true }) %>
<% } %>
